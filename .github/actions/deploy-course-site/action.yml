# action.yml
name: 'Deploy Course Site'
description: 'Converts md->html with pandoc and pushes to a downstream repo.'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - site                  # subdir in this repo
    - 25f-comp110          # target github user/org
    - 25f-comp110.github.io # target github repo